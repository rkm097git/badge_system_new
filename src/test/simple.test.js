/**
 * Simple test file to check if the testing infrastructure works
 */

describe('Basic test', () => {
  it('should pass a simple assertion', () => {
    console.log('Running simple test');
    expect(true).toBe(true);
  });
});
